{{##def.macro:data:

  <h1>{{=data.title}} content</h1>
  <p class="intro">
    {{=data.intro}}
  </p>
  <em>
    from content api: <a href="/api/{{=data.slug}}.json">/api/{{=data.slug}}.json</a> and the {{=data.template }} template
  </em>

#}}
